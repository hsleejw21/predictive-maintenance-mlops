streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
plotly>=5.9
# (선택) PPT 재생성용
python-pptx>=0.6
# (선택) 아래는 pyproject extras 로도 설치 가능: pip install -e ".[serving,tracking,dev]"
#   추론 API : fastapi>=0.110  uvicorn>=0.29  pydantic>=2
#   실험 추적 : mlflow>=2.10
#   테스트/린트: pytest>=8  ruff>=0.4  httpx>=0.27
